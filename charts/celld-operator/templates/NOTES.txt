celld-operator is installed in {{ .Release.Namespace }}.

See docs/installation.md and docs/operations.md
for external IAM, storage, NetworkPolicy and recovery requirements.
Only one operator installation may own these cluster-wide fleet CRDs.
{{- if not .Values.networkPolicyEnforced }}
Provisioning remains blocked until networkPolicyEnforced=true after verifying CNI enforcement.
{{- end }}
{{- if not .Values.fleetNamespaces }}
No fleet namespaces configured: set fleetNamespaces or apply config/rbac/fleet-namespace.yaml
in each namespace that will hold CelldFleet resources.
{{- end }}
CRDs, fleet resources and permanent bucket reservations are not removed on Helm uninstall.
