---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: celldstoragereservations.celld.example.com
spec:
  group: celld.example.com
  names:
    kind: CelldStorageReservation
    listKind: CelldStorageReservationList
    plural: celldstoragereservations
    singular: celldstoragereservation
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CelldStorageReservation is a durable, cluster-wide tombstone.
          Never garbage collected.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              bucket:
                description: Dedicated S3 bucket permanently bound to this reservation.
                type: string
              fleetName:
                description: Name of the fleet that owns the reservation.
                type: string
              fleetNamespace:
                description: Namespace of the fleet that owns the reservation.
                type: string
              fleetUID:
                description: Exact Kubernetes UID of the owning fleet; recreating
                  a fleet with the same name does not transfer the reservation.
                type: string
              initialReplicas:
                description: |-
                  InitialReplicas binds the replica component of SpecHash before provisioning.
                  Optional only for reservations created before this field existed.
                format: int32
                maximum: 100
                minimum: 1
                type: integer
              specHash:
                description: Fingerprint of the original immutable fleet configuration
                  used to detect conflicting reuse.
                type: string
            required:
            - bucket
            - fleetName
            - fleetNamespace
            - fleetUID
            - specHash
            type: object
            x-kubernetes-validations:
            - message: storage reservations cannot be transferred or changed
              rule: self == oldSelf
        required:
        - spec
        type: object
    served: true
    storage: true
