---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: celldpreviews.celld.eric.dev
spec:
  group: celld.eric.dev
  names:
    kind: CelldPreview
    listKind: CelldPreviewList
    plural: celldpreviews
    shortNames:
    - cp
    singular: celldpreview
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.fleetName
      name: Fleet
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .status.expiresAt
      name: Expires
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CelldPreview provisions an isolated application fleet with a bounded lifetime.
          Application code is uploaded separately to its isolated prefix in the pool bucket.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              poolRef:
                description: Shared platform configuration in this namespace; immutable.
                properties:
                  name:
                    description: Pool in the same namespace as the preview.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: preview pool reference is immutable
                  rule: self == oldSelf
              revision:
                description: Optional informational revision supplied by CI, not deployment
                  verification.
                maxLength: 256
                type: string
              seed:
                description: Optional one-time multi-object initialization; omission
                  starts empty.
                properties:
                  alarms:
                    default: Clear
                    description: Clear disables copied alarms. Preserve explicitly
                      opts into scheduled work.
                    enum:
                    - Clear
                    - Preserve
                    type: string
                  objects:
                    items:
                      properties:
                        class:
                          description: Exported Durable Object class, with the same
                            mapping in the target application.
                          maxLength: 128
                          minLength: 1
                          pattern: ^[A-Za-z0-9_$.-]+$
                          type: string
                        id:
                          description: Canonical object ID, not an idFromName input
                            or a storage path.
                          maxLength: 256
                          minLength: 1
                          pattern: ^[A-Za-z0-9_$.-]+$
                          type: string
                      required:
                      - class
                      - id
                      type: object
                    maxItems: 100
                    minItems: 1
                    type: array
                    x-kubernetes-list-map-keys:
                    - class
                    - id
                    x-kubernetes-list-type: map
                  source:
                    description: Administrator-approved alias from the pool's seeding.sources.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - objects
                - source
                type: object
              source:
                description: Human-readable branch or pull request identifier. Never
                  used as a resource name.
                maxLength: 256
                minLength: 1
                type: string
              ttlSeconds:
                default: 86400
                description: Lifetime from creation; immutable. Expiry initiates safe
                  fleet deletion.
                format: int32
                maximum: 604800
                minimum: 60
                type: integer
                x-kubernetes-validations:
                - message: ttlSeconds is immutable
                  rule: self == oldSelf
            required:
            - poolRef
            - source
            type: object
            x-kubernetes-validations:
            - message: preview seed selection is fixed at creation
              rule: has(self.seed) == has(oldSelf.seed) && (!has(self.seed) || self.seed
                == oldSelf.seed)
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: Internal HTTP endpoint; Ready confirms infrastructure,
                  not application deployment.
                type: string
              expiresAt:
                format: date-time
                type: string
              fleetName:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Initializing
                - Ready
                - Blocked
                - Deleting
                - Expired
                type: string
              poolName:
                type: string
              poolUID:
                type: string
              seedName:
                description: Initialization request, retained separately from the
                  preview.
                type: string
              seedPhase:
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Canceled
                type: string
              storageURL:
                description: Target for celld deploy; contains no credentials.
                type: string
              url:
                description: Unique URL; configure wildcard DNS and TLS on the selected
                  edge controller.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
